# Be sure to restart your server when you modify this file.

# Your secret key is used for verifying the integrity of signed cookies.
# If you change this key, all old signed cookies will become invalid!

# Make sure the secret is at least 30 characters and all random,
# no regular words or you'll be exposed to dictionary attacks.
# You can use `rake secret` to generate a secure secret key.

# Make sure the secrets in this file are kept private
# if you're sharing your code publicly.

development:
  secret_key_base: e2adc572fb8c6d24b0a8975e9e25b04711b4af9722f60808882a1fe30ddd27ef621354e844d6413fc460a76c4f7fb38d3f1689fbb0559c6ab8ec7d1917d2fd5a
  
  sorcery_github_callback_url: http://localhost:3000/oauth/callback?provider=github
  sorcery_github_key: <%= ENV["SORCERY_GITHUB_KEY"] %>
  sorcery_github_secret: <%= ENV["SORCERY_GITHUB_SECRET"] %>
test:
  secret_key_base: 05c8f001b5e9e9511490df42d5e44ddb099a2821cc626c3c4a89be273615e4bfc47b3ecd0aae62d5ccfddffa80b06363866cc739fa3e8c795cfddde4af8f3db4
  sorcery_github_key: <%= ENV["SORCERY_GITHUB_KEY"] %>
  sorcery_github_secret: <%= ENV["SORCERY_GITHUB_SECRET"] %>

# Do not keep production secrets in the repository,
# instead read values from the environment.
production:
  secret_key_base: <%= ENV["SECRET_KEY_BASE"] %>

  sorcery_github_callback_url: http://localhost:3000/oauth/callback?provider=github
  sorcery_github_key: <%= ENV["SORCERY_GITHUB_KEY"] %>
  sorcery_github_secret: <%= ENV["SORCERY_GITHUB_SECRET"] %>