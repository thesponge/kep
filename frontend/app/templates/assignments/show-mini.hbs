<div class="row">
  <div class="small-1 columns">&nbsp;</div>
  <div class="small-10 columns content">
    <h1 class="content-title">
      {{model.title}}
    </h1>
    <div class="row">
      <div class="small-9 columns"><hr></div>
      <div class="small-3 columns">
        {{!--
        <button class="button micro radius success"
          {{action 'match'}}>
          Match
        </button>
        --}}
        {{#if match}}
          <button class="button micro radius secondary"
            {{action 'matchExit'}}>
            Exit match
          </button>
        {{/if}}
      </div>
    </div>

    <ul class="small-block-grid-2 details-list">
      <li>
        <div class="panel radius callout">
          <ul>
            {{#each type in model.assignment_types}}
              <li>
                <span class="list-icon">
                  {{fa-icon type.icon}}
                </span> 
                {{type.option}} 
                <small> ({{type.category}})</small>
              </li>
            {{/each}}
          </ul>
        </div>
      </li>
      <li>
        <div class="panel radius callout">
          <span class="list-icon">
            {{#if model.travel}} {{fa-icon 'check'}}
            {{else}} {{fa-icon 'close'}} {{/if}}
          </span>
          Travel
          <br>
          <span class="list-icon">
            {{#if model.driver_license}} {{fa-icon 'check'}}
            {{else}} {{fa-icon 'close'}} {{/if}}
          </span>
          Driving license
          <br>
        </div>
      </li>
    </ul>

    {{!--edit-buttons 'compare' assignment=model.user_id user=session.content.id--}}
    {{#if model.is_owner}}
      <ul class="button-group radius pull-right">
        <li><a href="#" class="button tiny" {{action 'editAssignment'}}>Edit</a></li>
        <li><a href="#" class="button tiny alert" {{action 'deleteAssignment'}}>Delete</a></li>
      </ul>
    {{/if}}
  </div>
  <div class="small-1 columns">&nbsp;</div>
</div>

<div class="row">
  {{outlet 'match'}}
</div>

