<div class="contain-to-grid sticky">
    <nav class="top-bar" data-topbar>
        <ul class="title-area">
            <li class="name">
                <h1>
                  {{#link-to 'index'}}KEP{{/link-to}}
                </h1>
            </li>
        </ul>
        <section class="top-bar-section">
            <!-- Right Nav Section -->
            <ul class="right">
                <li>
                  {{#link-to 'candy'}}Candy{{/link-to}}
                </li>
                <li>
                  {{#link-to 'protected'}}Access the system{{/link-to}}
                </li>
                <li class="divider"></li>
                {{#if session.isAuthenticated}}
                  <li>
                      <li class="has-dropdown">
                          <a href="#">{{session.content.user_email}}</a>
                          <ul class="dropdown">
                            <li>
                              <a {{ action 'invalidateSession' }}>Logout</a>
                            </li>
                          </ul>
                      </li>
                  </li>
                {{else}}
                      <!-- {{#link-to 'login' data-reveal-id="loginModal"}}Login{{/link-to}} -->
                      <!-- <a href="#">[guest user]</a> -->
                {{/if}}
            </ul>
        </section>
    </nav>
</div>

<div class="row"  style="margin-top: 50px; min-height: 400px;">
  {{outlet}}
</div>
