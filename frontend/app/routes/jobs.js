import Ember from 'ember';

export default Ember.Route.extend({
  //model: function() {
  //  return Request.find();
  //}
  //model: function() {
  //  var jobs = this.store.find('job')
  ////  jobs.reload();
  //  return jobs;
  //}
});
