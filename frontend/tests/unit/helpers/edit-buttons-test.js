import {
  editButtons
} from '../../../helpers/edit-buttons';
import { module, test } from 'qunit';

module('EditButtonsHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = editButtons(42);
  assert.ok(result);
});
